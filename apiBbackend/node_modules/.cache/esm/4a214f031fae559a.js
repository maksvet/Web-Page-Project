let express,routes,dotenv,cors;_1fa‍.x([["default",()=>_1fa‍.o]]);_1fa‍.w("express",[["default",["express"],function(v){express=v}]]);_1fa‍.w("./src/auth_routes.js",[["default",["routes"],function(v){routes=v}]]);_1fa‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_1fa‍.w("cors",[["default",["cors"],function(v){cors=v}]]);




dotenv.config()

const app = express();
const port = process.env.PORT

app.use(cors());
app.use(express.json());
app.use('/', routes);
app.use((req, res, next) => {
    return res.status(404).send({
    status: 404,
    message: "not found"
    })
    next(error)
})

// error handler middleware
app.use((error, req, res, next) => {
    return res.status(error.status || 500).send({
        error: {
        status: error.status || 500,
        message: "Internal Server Error",
        },
    });
});

_1fa‍.d(app.listen(port, function () {
  console.log(`API server ready on http://localhost:${port}`);
}));